import React from 'react'

const Students = (props) => {
  return (
    <div>
        <p>name: {props.name}</p>
        <p>name: {props.name}</p>
        <p>name: {props.name}</p>

    </div>
  )
}

export default Students