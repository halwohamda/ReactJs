import React from 'react'

const Students = (props) => {
  return (
    <div>
        <p>name: {props.name}</p>
        <p>Age: {props.age}</p>
        <p>name: {props.name}</p>

    </div>
  )
}

export default Students